//function loadFile(event) {
 // var image = document.getElementById('output');
 // image.src = URL.createObjectURL(event.target.files[0]);
  //
 // var imageUrlInput = document.getElementById('image_url_input');
 // imageUrlInput.value = image.src; // Set the image URL to the hidden input field
//}
