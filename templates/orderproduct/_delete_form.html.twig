
<form method="post" action="{{ path('app_orderproduct_delete', {'idOrder': orderproduct.idOrder,'id_user': id_user}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orderproduct.idOrder) }}">
    <button class="btn btn-secondary">Delete</button>
</form>
