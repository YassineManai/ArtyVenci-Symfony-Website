
<form method="post" action="{{ path('app_auction_delete', {'idAuction': auction.idAuction , 'id_user':id_user}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auction.idAuction) }}">
    <button class="btn">Delete</button>
</form>
